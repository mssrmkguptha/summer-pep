class Solution {
  public:
  void segregate0and1(vector<int> &nums) {
        int n=nums.size();
        int i=n-1;
        int j=n-1; 
        
        while(i>=0){

            if(nums[i]==0) i--;
            else{
                swap(nums[i--],nums[j--]);
            } 
     }
  }
 };
