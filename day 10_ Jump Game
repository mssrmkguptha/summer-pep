class Solution {
public:
    bool canJump(vector<int>& nums) {
        int m = 0;
        int n=nums.size();
        for(int i=0;i<n;i++){
            if(m>=n-1) return 1;
            if(i>m) return 0;
            m=max(m,i+nums[i]);
        }
        return 1;
    }
};
