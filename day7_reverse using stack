class Solution {
  public:
    string reverse(const string& S) {
        stack<char> st;

        for (char x : S) {
            st.push(x);
        }

        string ans = "";

        while (!st.empty()) {
            ans += st.top();
            st.pop();
        }

        return ans;
    }
};
